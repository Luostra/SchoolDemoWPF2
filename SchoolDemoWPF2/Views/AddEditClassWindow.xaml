<Window x:Class="SchoolDemoWPF2.Views.AddEditClassWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchoolDemoWPF2.Views"
        mc:Ignorable="d"
        Title="AddEditClassWindow" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Class Name:" Style="{StaticResource ModernLabelStyle}"/>
        <TextBox x:Name="NameTextBox" Grid.Row="1" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Height="20" HorizontalAlignment="Right" Margin="0,10,0,-5">
            <Button Content="Save" Width="80" Margin="5,0" Click="SaveButton_Click" Style="{StaticResource SuccessButtonStyle}"/>
            <Button Content="Cancel" Width="80" Margin="5,0" Click="CancelButton_Click" Style="{StaticResource OrangeButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
